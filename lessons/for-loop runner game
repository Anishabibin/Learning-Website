<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loop Runner Game</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f8f9fa;
        }
        .game-container {
            width: 80%;
            margin: auto;
            padding: 20px;
        }
        .track {
            width: 600px;
            height: 100px;
            border: 2px solid black;
            background-color: lightgray;
            position: relative;
            margin: 20px auto;
        }
        .runner {
            width: 40px;
            height: 40px;
            background-color: red;
            position: absolute;
            top: 30px;
            left: 10px;
            border-radius: 50%;
        }
        .editor-container {
            text-align: left;
            width: 50%;
            margin: auto;
            background: #272822;
            padding: 10px;
            border-radius: 5px;
            color: white;
        }
        textarea {
            width: 100%;
            height: 80px;
            font-family: monospace;
            background: #272822;
            color: white;
            border: none;
            resize: none;
        }
        .btn {
            padding: 10px;
            background: #28a745;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            margin-top: 10px;
        }
    </style>
</head>
<body>

    <h1>🏃‍♂️ Loop Runner Game 🏁</h1>
    <p>Edit the loop to make the character reach the finish line!</p>

    <div class="game-container">
        <div class="track">
            <div id="runner" class="runner"></div>
        </div>
        
        <div class="editor-container">
            <h3>📝 Edit the Loop:</h3>
            <textarea id="codeEditor">
for (let i = 0; i < 5; i++) {  
    moveRight();  
}
            </textarea>
            <button class="btn" onclick="runCode()">Run Code</button>
        </div>
    </div>

    <script>
        function moveRight() {
            let runner = document.getElementById("runner");
            let currentPosition = parseInt(runner.style.left) || 10;
            runner.style.left = (currentPosition + 50) + "px";
        }

        function runCode() {
            let code = document.getElementById("codeEditor").value;
            document.getElementById("runner").style.left = "10px"; // Reset position

            try {
                eval(code); // Execute user input
            } catch (error) {
                alert("❌ Error in your code: " + error.message);
            }
        }
    </script>

</body>
</html>
